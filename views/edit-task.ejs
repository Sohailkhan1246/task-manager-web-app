<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Task</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">🗂️ Task Manager</a>
      <span class="navbar-text text-light">Edit Task</span>
      <div class="ms-auto">
        <a href="/dashboard" class="btn btn-outline-light btn-sm">Back to Dashboard</a>
      </div>
    </div>
  </nav>

  <!-- Form Section -->
  <div class="container mt-5">
    <h2 class="text-center mb-4">Update Task</h2>
    <form method="POST" action="/edit-task/<%= task._id %>" class="card p-4 shadow-sm mx-auto" style="max-width: 600px;">
      <div class="mb-3">
        <label for="title" class="form-label">Task Title</label>
        <input type="text" id="title" name="title" class="form-control" placeholder="Enter task title" value="<%= task.title %>" required />
      </div>
      <div class="mb-3">
        <label for="description" class="form-label">Description</label>
        <input type="text" id="description" name="description" class="form-control" placeholder="Enter description" value="<%= task.description %>" required />
      </div>
      <button type="submit" class="btn btn-warning w-100">Update Task</button>
    </form>
  </div>

  <!-- Footer -->
  <footer class="bg-primary text-white text-center py-3 mt-5 fixed-bottom">
    <small>© 2025 Task Manager | Built with ❤️ by Sohail</small>
  </footer>

</body>
</html>
